# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# Virtual environments
.venv/
venv/
env/

# Cached OpenStreetMap graph downloads (rebuilt on demand)
data/osm_cache/

# OSMnx's own cache of raw Overpass API responses. Regenerated automatically.
# `cache/` is where OSMnx puts it by default, if anything runs before
# load_place_graph() has redirected it to data/overpass_cache/.
data/overpass_cache/
cache/

# Benchmark scratch output. The figures in docs/images/ are generated by
# `python examples/make_figures.py` but ARE committed - the README embeds them.
benchmark_output/

# Testing
.pytest_cache/
.coverage
htmlcov/

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Node (if frontend tooling is added later)
node_modules/

# Local planning notes - not part of the published project.
PLAN.md
CONTRACTION_HIERARCHIES_PLAN.md
